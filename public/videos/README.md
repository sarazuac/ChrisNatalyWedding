- `ocean.mp4` — homepage hero background (`/`).
- `seashore.mp4` — banner on the Ship page (`/ship`).

Tips for the best result:
- Keep each under ~15–20MB — trim to a short, seamless loop (10–20s) rather than a long clip.
- 1920x1080 (or larger) landscape footage works best; it's cropped to fill the screen on all devices.
- If a file is missing, that page gracefully falls back to the ocean-toned gradient background.
